name: Extract Minecraft Textures

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Minecraft version (e.g., 1.21.7)"
        required: true
        default: "1.21.7"

jobs:
  extract:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: pip install -r extractor/requirements.txt

      - name: Extract textures
        run: python extractor/extract.py "${{ github.event.inputs.version }}" extracted_textures

      - name: Remove previous release and tag if exists
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          TAG="${{ github.event.inputs.version }}"

          # Delete release if exists
          if gh release view "$TAG" &>/dev/null; then
            echo "Release with tag $TAG exists. Deleting..."
            gh release delete "$TAG" --yes
          else
            echo "No existing release for tag $TAG"
          fi

          # Delete tag if exists
          if git ls-remote --tags origin | grep -q "refs/tags/$TAG"; then
            echo "Tag $TAG exists. Deleting..."
            git push origin --delete "$TAG"
          else
            echo "No existing tag $TAG"
          fi

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "${{ github.event.inputs.version }}"
          name: "Minecraft Data ${{ github.event.inputs.version }}"
          body: "Automated release for Minecraft version ${{ github.event.inputs.version }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Zip to Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "${{ github.event.inputs.version }}"
          files: extracted_textures/mc-data.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
